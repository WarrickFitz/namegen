{
  "name": "namegen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "namegen": "npm run build && node ./dist/index.js",
    "testname": "npm run build && node ./dist/testname.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eosjs": "^20.0.0",
    "node-fetch": "^3.1.1"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "@types/node-fetch": "^2.5.0",
    "typescript": "^3.5.3"
  }
}
